$(function () {

   $('.mainSlide').slick({
       arrows:true, 
       autoplay:true,
       autoplayspeed:20000,
       dots:true,
   });

    $('main-visual sliderArrows i:first-child').on ('click',function(){
    });
});